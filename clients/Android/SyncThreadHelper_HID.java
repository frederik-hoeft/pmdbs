package com.example.rodaues.pmdbs_navdraw;
/*
public class SyncThreadHelper_HID implements Runnable{
    // --------------SINGLETON DESIGN PATTERN START
    private static SyncThreadHelper_HID sync = null;

    private SyncThreadHelper_HID() {}

    private static String[] params = null;

    public static SyncThreadHelper_HID GetInstance(String[] inputparams)
    {
        params = inputparams;
        if(sync == null)
        {
            sync = new SyncThreadHelper_HID();
        }
        return sync;
    }
    // --------------SINGLETON DESIGN PATTERN END
    public void run()
    {
        Sync.setHid(params);
    }
}*/




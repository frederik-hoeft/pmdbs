- PasswordManager v2
	SERVER TODO:
		- TESTING:
			Nothing :)
			
		- OPTIMIZATION:
			Nothing :)
			
		- HIGH PRIORITY:
			- Update Docs
			- FIX SECURITY ISSUES:
				- add HMAC to prevent padding oracle attacks
				- derive HMAC key from hashed AES --> a == HMAC_key[:Len(HMAC_key) / 2] b == HMAC_key[Len(HMAC_key) / 2:]
				
		- MEDIUM PRIORITY:
			Nothing :)
			
		- LOW PRIORITY (FINISH CLIENTS FIRST):
			- Change admin setup:
				- one "__ROOT__" user ("master admin")
				- __ROOT__ can promote users to become admin 
				--> better maintenance for an admin team
			- Admin features:
				- delete accounts
